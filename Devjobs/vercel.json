{
  "builds": [
    {
      "src": "Devjobs/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/devjobs"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}