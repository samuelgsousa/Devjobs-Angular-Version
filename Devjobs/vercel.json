{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/devjobs"
      }
    }
  ],
  "routes": [
    { "src": "/Devjobs", "dest": "/" }
  ]
}