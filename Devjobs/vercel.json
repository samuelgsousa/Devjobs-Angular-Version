{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/devjobs"
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "Devjobs/src/index.html" }
  ]
}